# Write your MySQL query statement below
select r.contest_id, round(count(*) / (select count(*) from Users) * 100, 2) as percentage from Users as u join Register as r on r.user_id = u.user_id group by r.contest_id order by percentage desc, r.contest_id;